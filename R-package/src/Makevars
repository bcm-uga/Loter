# package root and dir
PKGROOT=.
DIR_SRC=src_libloter

PKG_CXXFLAGS= $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS= $(SHLIB_OPENMP_CXXFLAGS)
SOURCES= $(wildcard src_libloter/*.cpp src_libloter/*/*.cpp)
OBJECTS= hap_R.o $(SOURCES:.cpp=.o)
